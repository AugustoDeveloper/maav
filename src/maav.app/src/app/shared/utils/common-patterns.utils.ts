export const CommonPatterns = {
    onlyLetter: /^([a-zA-Z]+)$/,
    onlyNumbers: /^([0-9]+)$/,
    onlyLetterNumbers: /^([a-zA-Z0-9\-]+)$/,
    onlyLetterNumbersDots: /^([a-zA-Z0-9\.]+)$/,
    onlyLetterNumbersHyphen: /^([a-zA-Z0-9\-]+)$/
};
